import React from "react";
import { Container } from 'semantic-ui-react';
import './Layout.css';

const Layout = (props) => {
  return (
    <div>
Smart Home Devices
    </div>
  )
};

export default Layout;